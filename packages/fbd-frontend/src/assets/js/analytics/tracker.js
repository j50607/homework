class Tracker {
  /**
   * Example Tracker.visitTransactionRecordPage(Category (Required), Action (Required), Name (Optional – Recommended), Value (Optional))
   */
  // static visitTransactionRecordPage(category, action, name, value) {
  //   window._paq = window._paq || [];
  //   const parameters = [];
  //   parameters.push('trackEvent', category, action, name);
  //   if (typeof value === 'number') {
  //     parameters.push(value);
  //   }
  //   window._paq.push(parameters);
  // }
}

export default Tracker;
